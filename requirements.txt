ib_insync==0.9.86
python-dotenv==1.0.1
tzdata>=2024.1
pandas>=2.0.0
yfinance>=0.2.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
# Gateway console WebSocket reverse proxy.
websockets>=12.0,<14.0
# websockify is installed as a system package via apt (not pip) on the VPS.
# apt install websockify (0.10.0) — do not add it here.
pandas_market_calendars>=4.3.0
